#ifndef COMPOSITE_HPP
#define COMPOSITE_HPP

#include<iostream>
#include<string>
#include<vector>

#include<fstream>
#include<cctype>
#include<filesystem>
#include<cstdlib>
extern std::string termux;

#endif


void teminal();
void composite();
void icone();
void maker(int &choice);

bool make_model();
bool make_view();
bool make_controller();
void printer_other_choice(std::vector<std::string> option);